{
  "input": {
    "question": "describe this repo",
    "candidates": [
      { "skillId": "code.source.repo.describe", "rationale": "fixture", "mustHaveScope": "repo" }
    ],
    "repo": { "path": "/repo/alpha", "primaryLanguages": ["typescript"] }
  },
  "expectedConfidenceFloor": "low",
  "expectedConfidence": "low",
  "_comment": "Smoke gate: select-scope is cloud-routed; without a fakeProvider the LLM call throws and the skill returns `emptyOutput()` at confidence 'low'. Happy-path coverage (with capture-mode + canned-JSON fakeProvider) lives in code.meta.test.ts."
}
