{
  "input": {
    "question": "Describe the schema of the orders table on prod-db.",
    "candidates": [
      {
        "skillId": "data.source.rdbms.describe-table",
        "rationale": "RDBMS schema introspection.",
        "mustHaveScope": "connection+target"
      }
    ],
    "connections": [
      { "id": "prod-db", "family": "rdbms", "kind": "postgres" }
    ]
  },
  "expectedConfidenceFloor": "low",
  "expectedConfidence": "low",
  "_comment": "Smoke gate runs without a real LLM (FakeProvider's complete() throws), so the skill correctly falls through to its degraded path: confidence: 'low' with an LLM-failure note. Happy-path coverage lives in daemon/skills/__tests__/data.meta.select-scope.test.ts."
}
