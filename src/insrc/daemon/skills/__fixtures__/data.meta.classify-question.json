{
  "input": {
    "question": "Describe the schema of the orders table on connection prod-db.",
    "connections": [
      { "id": "prod-db", "family": "rdbms", "kind": "postgres" }
    ]
  },
  "expectedConfidenceFloor": "low",
  "expectedConfidence": "low",
  "_comment": "Smoke gate runs without a real LLM (the harness's default FakeProvider's complete() throws), so the skill correctly falls through to its degraded path: confidence: 'low' with an LLM-failure note. Happy-path coverage lives in daemon/skills/__tests__/data.meta.classify-question.test.ts which injects a fakeProvider returning canned JSON."
}
